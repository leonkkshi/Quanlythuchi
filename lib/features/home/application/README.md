# Home Application Layer

This folder is reserved for application logic (e.g. providers, use cases) specific to the Home feature.
Currently, the Home feature relies on providers and services from the Auth, Category, and Transaction features.
